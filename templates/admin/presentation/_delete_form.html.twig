<form method="post" action="{{ path('admin_delete_presentation_person', {'id': person.id}) }}" onsubmit="return confirm('Supprimer ce membre de la présentation ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ person.id) }}">
    <button class="btn p-0"><i class="far fa-trash-alt text-danger"></i></button>
</form>
